// Contratos de evento do Kerno. Movidos para @kerno/contracts (pacote puro,
// consumível por backend/frontend/apps cliente). Mantido como re-export para
// não quebrar os imports existentes via `@kerno/core/types`.

export * from "@kerno/contracts/events";
